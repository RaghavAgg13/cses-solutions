#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
    
int n, k;

int main(void) {
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    cin >> n >> k;

    vector<int> a(n);
    for (auto x : a) cin >> x;

    for (int i = 0; i < n; i++) {

    }
}